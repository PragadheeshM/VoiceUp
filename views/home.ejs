<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Government Grievance Portal</title>
    <style>
        :root {
            --primary-color: #73003b;
            --accent-color: #ffcc00;
        }
        * {
            box-sizing: border-box;
            scroll-behavior: smooth;
        }
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
            color: #333;
        }
        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: var(--primary-color);
            padding: 15px 30px;
            color: white;
        }
        .navbar ul {
            list-style: none;
            display: flex;
            gap: 20px;
            margin: 0;
            padding: 0;
        }
        .navbar ul li a {
            color: white;
            text-decoration: none;
            font-weight: bold;
        }
        .navbar ul li a:hover {
            text-decoration: underline;
        }
        .btn {
            background: var(--accent-color);
            border: none;
            padding: 10px 18px;
            border-radius: 5px;
            cursor: pointer;
            font-weight: bold;
            transition: background 0.3s ease;
        }
        .btn:hover {
            background: #e6b800;
        }
        .hero {
            text-align: center;
            padding: 80px 20px;
            background: url('https://via.placeholder.com/1500x500') no-repeat center center/cover;
            color: white;
        }
        .hero h2 {
            font-size: 2.5rem;
            margin-bottom: 10px;
        }
        .hero p {
            font-size: 1.2rem;
        }
        .status {
            text-align: center;
            padding: 40px 20px;
            background-color: #fff;
        }
        .status h2 {
            margin-bottom: 20px;
        }
        .status input {
            padding: 12px;
            width: 300px;
            max-width: 90%;
            border-radius: 5px;
            border: 1px solid #ccc;
        }
        .status .btn {
            margin-left: 10px;
        }
        .services {
            text-align: center;
            padding: 60px 20px;
        }
        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
            gap: 20px;
            max-width: 1000px;
            margin: auto;
        }
        .service {
            background: white;
            padding: 25px;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            font-weight: bold;
        }
        footer {
            text-align: center;
            padding: 15px;
            background: var(--primary-color);
            color: white;
            font-size: 0.9rem;
        }
        .emblem {
            width: 60px;
            height: auto;
        }
    </style>
    <script>
        function trackStatus() {
            var complaintId = document.getElementById("complaintId").value;
            if (complaintId) {
                window.location.href = "/currentStatus?id=" + complaintId;
            } else {
                alert("Please enter a Complaint ID");
            }
        }
    </script>
</head>
<body>
    <header>
        <div class="navbar">
          <img src="https://www.presentations.gov.in/logos/national-emblem/" class="emblem">

            <h1>Govt Grievance Portal</h1>
            <nav>
                <ul>
                    <li><a href="/">Home</a></li>
                    <li><a href="/currentStatus">Current Status</a></li>
                    <li><a href="/services">Services</a></li>
                    <li><a href="#contact">Contact</a></li>
                    <li><a class="btn login" href="/login">Login</a></li>
                    <li><button class="btn signup">Sign Up</button></li>
                </ul>
            </nav>
        </div>
    </header>

    <section id="home" class="hero">
        <h2>Efficient Grievance Redressal</h2>
        <p>Submit complaints and track resolution status easily.</p>
    </section>

    <section id="status" class="status">
        <h2>Check Your Grievance Status</h2>
        <input type="text" id="complaintId" placeholder="Enter Complaint ID" />
        <button class="btn" onclick="trackStatus()">Track</button>
    </section>

    <section id="services" class="services">
        <h2>Our Services</h2>
        <div class="grid">
            <div class="service">Banking</div>
            <div class="service">Transport</div>
            <div class="service">Education</div>
            <div class="service">Health</div>
        </div>
    </section>

    <footer>
        <p>&copy; 2025 Government of India | All Rights Reserved</p>
    </footer>
</body>
</html>